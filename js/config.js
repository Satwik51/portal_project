const CONFIG = {
  USE_MOCK_API: true, // Keep true so local DB works
  USE_REAL_EMAIL: true, // Uses Node.js Backend for Email (Nodemailer)
  API_BASE_URL: 'http://localhost:3000/api/v1'
};

export default CONFIG;
